general {
    # Real system suspend only. Automatic DPMS blanking is OFF on this host
    # (dual-GPU: blanking HDMI-A-1 left both panels dark / session unusable
    # 2026-07-27 — ADR 20260727-hypr-monitor-heal-portrait-crtc).
    after_sleep_cmd = /bin/bash /home/kodex/.local/bin/hypr-monitor-heal after-sleep
    ignore_dbus_inhibit = false
    ignore_systemd_inhibit = false
}

# No idle listener. Do not re-add DPMS off for HDMI-A-* without a single-GPU layout.
