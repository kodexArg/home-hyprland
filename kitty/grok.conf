# Grok CLI only (Super+G / class grok-cli).
# Fullscreen TUI paints its own bg; opacity still softens the chrome if useful.
include kitty.conf

background_opacity 0.72
dynamic_background_opacity yes
background_image none
background #000000
foreground #e0e0e0
